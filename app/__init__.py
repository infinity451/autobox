# -*- coding: utf-8 -*-
"""AutoBox 自动化工具箱 —— 个人自动化平台。

本文件是 app 包的“身份证”（__init__.py 是 Python 包的固定文件名）。
它本身不需要写逻辑，存在即可，作用是告诉 Python：“app 这个文件夹是一个包”，
这样别的文件才能用 `from app.xxx import yyy` 的方式引用 app 下的代码。
"""

# 定义这个项目的版本号，方便以后发布时标识“这是第几个版本”
# __version__ 是常见惯例：别的代码可以直接 `import app; app.__version__` 查看版本
# 版本历史：0.1.0 规则管家 → 0.2.0 网页采集器 → 0.3.0 批量魔法+定时中心
#          → 0.4.0 宏录制器 → 0.5.0 打包 exe → 0.6.0 完全桌面化
__version__ = "0.6.0"
